#include <stdio.h>

// Function definition
int add(int a, int b) {
    return a + b;
}

int main() {
    
    // Function call
    int result = add(5, 3);
    printf("The sum is: %d", result);
    return 0;
}
